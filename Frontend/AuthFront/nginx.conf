server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Перенаправляет все запросы на index.html, чтобы React Router сам управлял путями
        try_files $uri $uri/ /index.html; 
    }
}
